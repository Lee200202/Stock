# 2026/09/12 驗證紀錄

## 原始資料與版本

- 0910.txt：23,493 字；SHA256 `60d5901a9e651190ff0b749bb6c2d75931a804a47711503a7b3ad7ed3d7e8227`。
- 0911.txt：19,731 字；SHA256 `48f8f564bbc2a23d7619b4c44ee58f4861e39ceecc80b95dabfa1f6a61fbcf16`。
- 兩份均與使用者提供的最後一版 Actions 日誌一致。
- 修改前完整程式與測試備份：`backups/before-0912-review.zip`。
- Code.gs 拆檔透過 JavaScript AST 核對：56 個原有頂層宣告全數保留；只有指定的行為／版本變更，不漏公開函式。原檔約 900 行，現在入口只保留路由與 include。

## 離線程式測試

執行 `python -X utf8 -m unittest discover -s tests -p 'test_*.py'`。

**155 項通過**。保留既有 145 項，另加 10 項：語意覆核補持股、覆核失敗保留原結果、候選不無聲消失、錯誤陣列、逾時與授權錯誤區別、舊文章教學更新、較長盤勢、純歷史不顯示、分類比較、持股／觀望分開比較。

既有單輪模式的測試改為明確設定 `GEMINI_SEMANTIC_AUDIT=false`；較短字數與自動拆黏連價格的舊期待，依新需求調整。沒有移除其他既有案例。

三個 Node 測試檔通過：

- `tests/test_gas.js`：23 個腳本語法檢查、證據、持久化逐日同步、失敗、取消、文章與同日防重寄。
- `tests/test_context_gas.js`：確認語音名稱、貨幣排除、矽智財等產業排除。
- `tests/test_review_gas.js`：績效待補資料／成功、all 分批續跑與失敗、同日期重入保護、相鄰欄位批寫、不覆寫間隔欄、进度數值與可及性、章節標題、HTML 文字轉義、郵件主題樣式。

根目錄與 pipeline/ 執行副本已同步。Prompt 共用規則由同一個同步腳本產生；官方證據格式與既有複審回應契約分別保留。

## 真實前端程式的本機瀏覽器測試

使用本機 Edge 的 headless 模式、Playwright，以及實際 Index／Admin／Stylesheet／JavaScript／MailService 渲染器。`google.script.run` 全部接本機替身，沒有對正式 Google 服務請求。

| 情境 | 結果 |
| --- | --- |
| 桌機 1440 × 1000 | 整頁 scrollWidth=1440；郵件區 1378px，三張表各 1298px，無頁面橫向溢出 |
| 手機 390 × 844 | 整頁 scrollWidth=390；郵件區 358px，表格容器 326px，表格內容 600px，只在表內滑動 |
| 會員持股 | 原有專區及完整表格仍存在 |
| 快速點 9/10 再 9/11 | 故意延遲 9/10 回應，最後仍顯示 9/11 |
| 減少動態效果 | 分頁 animationName=none |
| 一般動態設定 | 使用 rd-fade 漸入位移動畫 |
| 深色模式 | 郵件 shell 實際字色與閱讀區一致，修正原先黑字不可讀 |
| 後台待補資料 | 第 4/5 步顯示已完成 60%，無假完成；取消按鈕停用、保留待補原因 |
| 瀏覽器例外 | 桌機、手機與後台均 0 個 pageerror |

已目視檢查桌機表格、手機表格及深色模式截圖。截圖與數值在 `preview/`；`index.html` 可本機預覽。`mail.html` 是實際寄信 HTML 渲染器的輸出示範，不含寄信行為。

## 生成品質與線上功能的驗證界線

本機沒有載入 Gemini 金鑰，因此未進行新的真實模型判讀。本次 9/11 文章示範由獨立讀稿整理，用於內容深度與排版比較，**不是新模型實跑結果**。

`analysis-baseline.json` 是人工待確認的比較基準，尤其 9/11 力積電的共同主詞範圍需保留判讀彈性。比較器驗證的是名稱／代號與分類集合，不能取代對每句理由、價格與原文證據的人工覆核。

尚未在線上測試：GAS 部署與觸發器、真實 Sheets 寫入、Gemini 配額與延遲、Fugle／官方行情資料、Gmail／Outlook 客戶端、實際寄送。未將離線測試稱為完整端到端成功。

## 更新後建議執行

1. 同批更新所有 GAS 檔案並建立新版本，確認 ping build。
2. check_keys 測真實模型及 thinkingLevel。
3. 空日K先補，之後接續刷新檢查點。
4. 使用同一份 9/10、9/11 原稿重跑，匯出最終分類 JSON。
5. 以比較器列出差異，再核對原句；不要只看「幾檔」變多。
6. 保留舊寄送狀態；本次不需要為驗證而自動重寄已寄信件。
