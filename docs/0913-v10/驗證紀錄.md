# v10 離線驗證紀錄

日期：2026/09/13。版本：`2026-09-13-quality-v10`。

| 驗證 | 結果 |
| --- | --- |
| Python unittest discover | 253項通過；包括本版新增14項語氣、主詞、價格與持股測試 |
| Node `tests/test*.js` | 9支通過；含GAS語法、原文指紋、覆蓋、防重寄、503輪替、進度及公開說明 |
| Python兩份副本 | 完全一致，語法解析通過 |
| Prompt同步 | Python正本同步GAS擷取／覆核／複審與文件，已確認別名表同步GAS和前端 |
| 桌面1440px | 整頁1440px，郵件1378px，表格1298px，無整頁水平溢出 |
| 手機390px | 整頁390px，郵件358px；表格600px在326px容器內橫向捲動 |
| 非同步與進度 | 反序回應不蓋掉新選擇；待複核、等待日K為警示，背景未完不顯示全部成功 |
| 互動錯誤 | 績效請求失敗解除載入狀態；個股/K線失敗顯示錯誤；沒有瀏覽器例外 |
| 動畫 | 減少動態效果偏好下停用進場效果 |

新增測試直接重現 ETF 操作勤誠的900以下誤歸屬、譜瑞接到世芯漲跌數字、嘉澤會員持股被漏掉、華邦電中性換手偏多、宇瞻2379與宏準錯字、說明與原文證據隔離，以及相同教學換標題重複計數。測試中的逐字稿片段是已提供原文的情境，不呼叫模型猜測標準答案。

重跑：

```powershell
python -X utf8 scripts/sync_quality.py
python -X utf8 -m unittest discover -s tests -p 'test_*.py'
Get-ChildItem tests -Filter 'test*.js' | ForEach-Object { node $_.FullName }
$env:PREVIEW_OUTPUT='docs/0913-v10/preview'
node scripts/preview_0912.js
python -X utf8 scripts/package_quality_v10.py
```

瀏覽器預覽脚本使用本機已安裝的 Playwright／Edge 與實際HTML/CSS/JS模板，Google Script API 回應為 fixture。`preview/browser-checks.json` 是量測記錄；預覽文章只作版面驗證，不是正式 Gemini 重跑結果。

- [桌面表格](preview/desktop-table.png)
- [手機表格](preview/mobile-table.png)
- [等待日K進度](preview/admin-dailyk-waiting.png)
- [待複核警示](preview/admin-review-warning.png)

沒有驗證正式帳號的網路連線、當日模型配額或雲端觸發器實際執行。尚需依部署手冊重跑9/10與9/11，確認模型將分散段落的900買點正確接回勤誠，以及兩日所有持股與觀察名單。
