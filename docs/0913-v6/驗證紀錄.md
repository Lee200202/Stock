# v6 驗證紀錄

本機離線驗證；沒有讀寫正式試算表、派GitHub工單、寄信或呼叫真實Gemini。

- Python unittest：182項通過。保留v5原文指紋、同日重複、跨日來源保護、已寄送狀態、覆蓋保留舊資料、日K續跑等案例。原來「行情例子應排除」的測試依本次新規則改為保留觀望。
- 新增價位案例：最高值、以上／以下、千分位、管理者確認的黏連範例、技術線200、沒有日K、超區間不乘十補猜、交易原始多次價不被展示摘要覆蓋。
- 新增收錄案例：已確認名稱及ETF、貨幣排除、篇幅與排除清單檢查、追加覆核最多一次。
- 新增背景案例：先smsmail→codes→tracker，保存未完成檢查點；後段才執行perfhist→perf，不重做內容步驟。
- Node五組：`test_gas.js`、`test_context_gas.js`、`test_review_gas.js`、`test_overwrite_gas.js`、`test_quality_v6_gas.js` 通過。包含所有GAS及內嵌腳本語法、共享價位、盤中OHLC、過期／缺欄快取、正式K優先及不修改原快取陣列。
- Edge無頭瀏覽器：1440px桌面及390px手機、明暗配色、減少動態效果、六章信件及會員持股區；零頁面JavaScript例外。
- 桌面body=1440、郵件寬1378、表格容器1298；手機body=390、郵件寬358，表格在326px容器內橫向滑動600px，整頁不溢出。
- 實際介面事件反序回應：郵件日期、逐日查詢、個股標頭，不會由晚回的上一筆覆蓋；績效失敗解除骨架；K線失敗明示。
- 後台待複核、等待日K為警示；新增內容100%與背景未完成分列；整體進度仍低於100%。

可重跑：

```powershell
python -X utf8 scripts/sync_quality.py
python -X utf8 -m unittest discover -s tests -p "test_*.py"
node tests/test_gas.js
node tests/test_context_gas.js
node tests/test_review_gas.js
node tests/test_overwrite_gas.js
node tests/test_quality_v6_gas.js
$env:PREVIEW_OUTPUT='docs/0913-v6/preview'
node scripts/preview_0912.js
python -X utf8 scripts/package_quality_v6.py
```

預覽使用既有人工整理示範及假API回應，不能當成新Prompt實際產出。正式模型分類、完整篇幅、行情供應商及雲端續跑仍須依部署手冊逐項驗收。
