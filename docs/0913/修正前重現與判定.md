# 2026/09/13 修正前重現與判定

1. 同日影片清單先放真實 ID 舊原文，再放 MANUAL ID 新原文：existing_transcript 會先命中日期，讀錯舊稿；mark_status 在只有真實 ID 時會追加同日 MANUAL 列。確認需修。以最新投稿 SHA256 為權威，不能以「篇幅最長」代表最新，較短的新稿也必須生效。
2. 同一來源日期先以真實 ID 補記前日操作，再以 MANUAL ID 重跑：前日舊 ID 紀錄未被清除。確認需修；但不能清掉前日所有逐字稿，否則前日自身影片也會遭刪除。改為蒐集來源日期的歷代 ID，只刪這些來源的補記，保護簡訊與人工補登。
3. 稽核待確認且新筆數少於舊筆數：stage_extract 保留資料，run_admin_job 最後卻報全部更新成功。確認需修，需傳遞明確結果並以警示結束。
4. 同一影片日期有兩個來源 ID 的 published manifest：郵件會合併兩批摘要。確認需修，採該來源日期最後發布批次。
5. 已人工修改的原逐字稿列仍保留來源 ID，重跑會覆蓋；新增 MANUALENTRY 列才保護。這是既有語意，介面明示；覆蓋閘門改按日期檢查紀錄及已完成影片。
6. 日 K 空快取：perfhist 回 pending，刷新函式拋例外而 GitHub exit 1。確認需補相依資料並保存待續跑狀態，不得恢復假成功。
7. 櫃買 TLS 錯誤目前退到快取有效，不能關閉憑證驗證；另檢查官方來源網址與 CSV 格式。

離線測試將覆蓋上述路徑。正式雲端資料尚未讀取或修改；使用者提供日誌為遠端觀察依據。
