# 2026/09/13 驗證紀錄

## 離線程式測試

- `python -X utf8 -m unittest discover -s tests -p "test_*.py"`：173項通過。
- `node tests/test_gas.js`：25段GAS/HTML script語法及既有證據、同步、取消、文章與防重寄測試通過。
- `node tests/test_context_gas.js`：諧音與排除條件通過。
- `node tests/test_review_gas.js`：依賴pending、分批刷新、欄位寫入、進度及HTML處理通過。
- `node tests/test_overwrite_gas.js`：SHA256跨語言一致、實際投稿、較短新原文、閘門、最新manifest、乾跑備份整併冪等性與分批上限、快照變更拒絕、日K續派／冷卻／上限／取消、橘色警示通過。
- 新增Python覆蓋測試涵蓋同日多列、ID缺失時更新同日列、工單指紋不符立即失敗、舊工單相容、保留舊資料結局、歷代ID跨日清除、完整write_results保留他日／簡訊／人工、日K部分完成與真錯誤、明確dailyk可繞過後台跳過條件、刷新續跑零AI健檢、CSV錯誤HTTP編碼及權證在前的ISIN。
- pipeline.py與pipeline/pipeline.py逐位元組相同；使用sync_quality.py同步GAS Prompt。

## 官方公開來源實測

2026/09/13本機標準requests、TLS驗證開啟：

- MOPS上櫃公司CSV：891檔，含4966譜瑞-KY。
- TWSE ISIN上櫃普通股：891檔，含4966譜瑞-KY，權證/ETF不納入。
- TPEx Swagger抓取曾收到不完整回應；沒有宣稱其伺服器憑證或連線問題已修好。

## 瀏覽器

`PREVIEW_OUTPUT=docs/0913/preview` 執行scripts/preview_0912.js，使用實際模板與CSS，google.script.run為本機假API。

- 桌面1440px：頁面寬1440、郵件內容1378、表格區1298，未溢出。
- 手機390px：頁面寬390、郵件內容358、表格容器326，表格600px可在區域內橫向滑動。
- 日期快速切換不顯示較舊回應；淺/深色及reduced-motion通過。
- 會員持股表格仍存在。
- 待複核與等待日K使用橘色進度條與說明邊線，等待狀態可取消；沒有假100%完成。
- 頁面沒有JavaScript錯誤；截圖及browser-checks.json置於preview/。

## 未驗證的遠端範圍

未部署GAS、未push GitHub、未更改正式Sheets及Triggers、未呼叫真實Gemini、未寄信。因此不能宣稱雲端工單已完成或新版AI實際分類已與人工答案一致。使用者提供的日誌是遠端既有狀態依據。
