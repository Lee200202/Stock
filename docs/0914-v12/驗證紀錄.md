# v12 離線驗證紀錄

版本：2026-09-14-quality-v12。Python 254項通過，Node測試程式11支通過。新測試見 `tests/test_article_v12.py` 與 `tests/test_article_v12_gas.js`。

- 盤勢與教學都缺三點時僅補問一次，補回的引用與數字逐項核對；重複點或憑空數字不收，來源不足留篇幅提醒。
- 20字內標題依已驗證盤勢；舊固定標題被替換，②基本資訊仍保留節目／講者／日期。
- 清除原文作14與川服(川湖)誤字括注，正確數字、會員持股事實及原始引用保留。
- 川湖跌600元不作股價600；有明確17880股價與820跌幅時保留17880。
- v11連線／HTML404重試、等待續跑、取消／限次、名称補正不當作刪除回歸通過。
- 覆蓋原文指紋、保護來源、既有寄送狀態、503金鑰輪替、個股反序回應及等待日K進度持續通過。
- 桌面1440px整頁不溢出；郵件1378px，表格1298px。手機390px整頁不溢出，表格600px在326px容器橫向捲動。
- 實際渲染模板中5個「未說明」儲存格均為nowrap；長文字仍正常換行。瀏覽器無例外，減少動態效果偏好生效。

重跑：

```powershell
python -X utf8 scripts/sync_quality.py
python -X utf8 -m unittest discover -s tests -p 'test_*.py'
Get-ChildItem tests -Filter 'test*.js' | ForEach-Object { node $_.FullName }
$env:PREVIEW_OUTPUT='docs/0914-v12/preview'
node scripts/preview_0912.js
python -X utf8 scripts/package_quality_v12.py
```

[瀏覽器量測](preview/browser-checks.json)／[桌面表格](preview/desktop-table.png)／[手機表格](preview/mobile-table.png)／[等待日K進度](preview/admin-dailyk-waiting.png)。

預覽用真實程式模板及模擬API，文章為排版fixture。沒有呼叫真實Gemini、行情API、寫正式試算表或寄信；實際模型仍須部署後重跑9/10、9/11驗收。
