# v69 手機表格、日曆與後台帳號位置：部署與驗證（2026/09/25）

Apps Script build：`2026-09-25-tables-v69`。ping 的 features 最前面是 `mobile-tables-v69`、`admin-account-topbar-v69`、`tracker-low-high-v68`。

**v69 包含還沒部署的 v68**（進出場一律取當日最低／最高、走勢圖標持有期間，見 [v68 部署與驗證](../0925-v68/部署與驗證.md)）。正式站目前是 v67（2026/09/25 18:16 ping 實測），直接部署 v69 即可，不用先部署 v68。

> 本次工具工作**沒有部署 Apps Script、沒有重算正式試算表、沒有寄信**。版面以本機預覽（正式模板＋假資料）驗證，手機寬度用真的觸控事件滑動測試。

## 一、管理者回報與原因

| 回報 | 原因 | 修正 |
|---|---|---|
| 手機上「說明重點」往右滑不動 | 個股面板「每天講了什麼」與個股查詢結果的外框同時掛 `scrollx` 與 `flush`；v54 的 `.card.flush { overflow: hidden }` 排在後面、權重相同，把左右捲動蓋掉。840px 的表格塞在 360px 的框裡，說明重點整欄被切掉，怎麼滑都不動 | 捲動還回來；這兩張表在手機（與「版面設定＝手機」）改成一筆一張卡片：日期與方向一列、價位一列（未說明就不佔）、說明整列，不需要左右滑 |
| 表格字太小 | 表頭 11.5px、代號與回合標籤 11px、明講灰字 11.5px | 表頭 13px，其餘 12～12.5px（乘字級設定） |
| 表格過窄 | 績效表手機 415px 放在 360px 裡、右邊被切；「追蹤檔數」斷成「追蹤檔／數」 | 固定欄寬、手機用短表頭（追蹤／持有／平均／正報酬） |
| 日曆和其他內容不對齊 | 後台 `.panel .mailcal button { min-height: 0 }`：沒有圓點的日子只剩 22px、有圓點的 46px，整月高低不齊 | 每一格固定同高（桌面 46、手機 38），休市斜線格也一樣 |
| 會員簡訊收錄清單（截圖） | 發文時間 132px 放不下，被切成「2026/09/2／4 09:33:38」；解析狀態膠囊不換行，壓到收錄個股 | 發文時間拆成日期、時間兩行；解析狀態改成可換行的灰字；欄寬重新分配，字級 13～14px。手機卡片：三層巢狀外框（卡片→工具框→子框）只留 254px 給內容，720px 以下攤平成 322px |
| 帳號圖示要在右上角 | v67 放在內容區右上方一列 | 移到頂列、版面設定齒輪左邊，與旁邊兩顆同樣式；登入後才出現 |

另外：會員簡訊「目前第 N 格」收合列原本被套成黑色主要按鈕，改成淺色列。

## 二、要部署的檔案（v67 → v69，8 檔）

- `.gs`：Config、Setup、SheetService
- `.html`：Admin、Changelog、Index、JavaScript、Stylesheet

部署包（只在本機）：`release/zhangzhen-tables-v69.zip`（8 檔）、`release/zhangzhen-full-v69.zip`（33 檔）。

## 三、部署步驟

1. 17:00 以後，在原 Apps Script 專案替換 8 檔並儲存。
2. 編輯器執行 `checkProjectFiles()`：`ok:true`、build `2026-09-25-tables-v69`。
3. 「部署 → 管理部署作業 → 編輯 → 新版本 → 部署」，開 `?action=ping` 確認。
4. **重算**（v68 的取價規則）：GitHub → Actions → `day-edit-sync` → `mode = performance`（回「已有更新工單」就改 `cancel-performance`）。
5. 不要執行 `installTriggers`、`dailyPushJob`、`repairDailyKCache`。

## 四、部署後核對（手機請用真的手機）

1. 點任一檔股票 → 最下面「從第一次提到到現在，每天講了什麼」：手機上一筆一張卡片，說明重點完整顯示，不需要左右滑。桌面照舊是表格。
2. 首頁搜尋一檔股票，結果也是卡片。
3. 績效走勢表格：手機五欄都看得到，表頭不斷字。
4. 後台右上角：首頁、帳號（人像）、版面設定三顆並排；點帳號出現「忘記這台裝置／登出」。
5. 後台投稿日曆：每一格同高。
6. 會員簡訊 → 載入收錄清單：桌面發文時間兩行、解析狀態不壓到隔壁；手機卡片幾乎滿版。
7. v68 的核對項目（力旺出場價＝9/23 最高、持有聲明進場＝聲明日最低）照 v68 文件第五節。

## 五、本機驗證

- 新增 `scripts/audit_tables_v69.js`：前後台每一頁在 390（觸控）、768（觸控）、1440 寬度量表格被切掉的文字、比外框寬又不能捲的表格、表格內小於 12px 的字、整頁橫向溢出，並對每個能左右捲的容器送一次真的觸控滑動確認會動。修正前 27 頁有問題（含個股面板表格 840px／360px 不能捲、績效表溢出、後台收錄清單手機滑不動、11px 小字）；修正後**全部通過**。
- Node 76 支、`check_mobile.js`、`scripts/preview_v54.js`（前台六種寬度＋強制手機、後台五種寬度）全部通過；新增 `tests/test_tables_v69.js`。
- 截圖在本機 `docs/0925-v69/preview/`（`rec-390.png`、`perftable-390.png`、`admin-smslist-*.png`、`admin-cal-*.png`、`admin-top-*.png`、`admin-acct-*.png`），不放 GitHub。
- 未驗證：正式站部署後的實機畫面（照第四節）。
